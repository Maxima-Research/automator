class config():
    def __init__(self,configLocation):
        if(configLocation == '')
            self.configFileName = 'config.txt')
        else:
            self.configFileName = configLocation

    def Load():
        
